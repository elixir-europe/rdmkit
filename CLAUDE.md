<!--
  Claude Code reads CLAUDE.md, not AGENTS.md, so this file exists only to pull in
  AGENTS.md. Keep the shared instructions in AGENTS.md so every assistant reads the
  same thing; add Claude-specific instructions below the import if you ever need any.
-->

@AGENTS.md
